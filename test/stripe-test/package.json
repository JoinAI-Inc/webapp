{
    "name": "cloudflare-stripe-subscription",
    "version": "1.0.0",
    "description": "Stripe subscription system with Cloudflare Workers",
    "main": "worker.js",
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy",
        "setup": "node setup-stripe-products.js",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "keywords": [
        "stripe",
        "subscription",
        "cloudflare",
        "workers"
    ],
    "author": "",
    "license": "ISC",
    "dependencies": {
        "stripe": "^20.1.0"
    },
    "devDependencies": {
        "wrangler": "^3.0.0"
    }
}
